# Copy this file to .env and fill in real values.
# .env is gitignored — never commit it.

# Apify API token (needed for Step 4 — SEEK adapter)
SECRET_KEY=generate_with__python3_-c__import_secrets;print(secrets.token_hex(32))

# Anthropic API key (needed for Step 6+ — CV extraction and matching)
ANTHROPIC_API_KEY=sk-ant-...

# Apify API token (needed for Step 4 — SEEK adapter)
APIFY_TOKEN=your_apify_token_here
